---

yaml-files:
  - '*.yaml'
  - '*.yml'
  - '.yamllint'

rules:
  indentation:
    spaces: 2
    indent-sequences: true
    check-multi-line-strings: true
  line-length:
    max: 120
    allow-non-breakable-inline-mappings: true
  new-lines:
    type: unix
